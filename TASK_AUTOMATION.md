# AI Task Automation System

## Обзор

Система автоматической обработки задач с помощью AI агентов для управления 3 бизнесами в Германии.

## Как это работает

### 1. Задачи в Dashboard

Все маркетинговые задачи хранятся в Supabase в таблице `decisions`:
- 23 задачи для немецкого рынка
- Статусы: `pending`, `approved`, `rejected`, `deferred`
- Каждая задача назначена определенному агенту

### 2. AI Агенты

Система использует 10+ специализированных агентов:

| Агент | Специализация | Задачи |
|-------|---------------|--------|
| **Marketing Agent** | Google Ads, SEO, Content | Рекламные кампании, контент-маркетинг |
| **Sales Agent** | B2B, Partnerships | Холодные рассылки, партнерства |
| **Content Agent** | Social Media, Copywriting | TikTok, Instagram, статьи |
| **Growth Agent** | Viral Growth, Referrals | Реферальные программы, эксперименты |
| **Product Strategy** | Product, Monetization | Подписки, pricing, roadmap |
| **PR Agent** | Media, CSR | Публикации в СМИ, соц.проекты |
| **Business Development** | Partnerships, M&A | Стратегические партнерства |
| **Legal Agent** | GDPR, Compliance | Юридические вопросы |
| **Development Agent** | Tech, Architecture | Разработка, AR, навигация |
| **Financial Agent** | Budgets, ROI | Финансовое моделирование |

### 3. Процесс обработки

```
1. Пользователь нажимает "Запустить AI агентов"
   ↓
2. Система берет 5 pending задач из Supabase
   ↓
3. Для каждой задачи определяется подходящий агент
   ↓
4. Агент получает задачу с контекстом:
   - Название и описание
   - Приоритет
   - Бизнес
   - Ожидаемый результат
   ↓
5. AI агент через Claude API:
   - Анализирует задачу
   - Учитывает специфику Германии
   - Создает детальный план
   - Рассчитывает метрики
   ↓
6. Результат сохраняется в:
   - activities (для истории)
   - decisions (статус → approved)
   ↓
7. Пользователь видит результаты в UI
```

## Использование

### API Endpoint

**POST** `/api/tasks/process`

#### Автоматическая обработка (5 задач):
```javascript
fetch('/api/tasks/process', {
  method: 'POST',
  body: JSON.stringify({ autoProcess: true })
})
```

#### Обработка конкретной задачи:
```javascript
fetch('/api/tasks/process', {
  method: 'POST',
  body: JSON.stringify({ taskId: 'task-id-here' })
})
```

**GET** `/api/tasks/process`
- Возвращает последние 20 обработанных задач

### UI Component

Откройте: **http://localhost:3000/tasks**

1. Нажмите "🤖 Запустить AI агентов"
2. Подождите 1-3 минуты
3. Увидите результаты обработки
4. Полные отчеты в разделе "Activities"

## Примеры результатов

### Marketing Agent
```markdown
АНАЛИЗ ЗАДАЧИ: Google Ads кампания (Германия)

ПЛАН ДЕЙСТВИЙ:
1. Keyword Research
   - "Handwerker Berlin" (5400 запросов/мес, CPC €2.50)
   - "Notdienst Klempner" (2900 запросов/мес, CPC €3.80)
   - Long-tail: "Handwerker in meiner Nähe"

2. Настройка кампании
   - Budget: €500/мес (€16.67/день)
   - Геотаргет: Berlin, München, Hamburg
   - Временной таргетинг: 8:00-20:00

3. Креативы
   - 3 варианта объявлений
   - A/B тест заголовков
   - Landing page оптимизация

ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ:
- 250 заказов/мес
- CPA: €2
- ROI: 600%

МЕТРИКИ:
- CTR: 5-7%
- Conversion Rate: 10%
- ROAS: 6:1
```

### Sales Agent
```markdown
СТРАТЕГИЯ: B2B Hausverwaltungen

ЭТАПЫ:
1. Холодная рассылка (День 1-7)
   - 1000 писем Hausverwaltungen
   - Персонализация по городу
   - Кейсы и testimonials

2. Follow-up (День 8-14)
   - Повторная рассылка
   - LinkedIn outreach
   - Телефонные звонки

3. Демо и закрытие (День 15-30)
   - 50 демо звонков
   - Корпоративные тарифы -20%
   - Договоры на 12 месяцев

ПРОГНОЗ:
- 20-30 клиентов B2B
- €30K MRR через 3 месяца
```

## Расширенные возможности

### Автоматический запуск (Cron Job)

Создайте файл `/api/cron/process-tasks/route.ts`:

```typescript
export async function GET() {
  // Запускается каждый день в 9:00
  const response = await fetch(
    `${process.env.NEXT_PUBLIC_APP_URL}/api/tasks/process`,
    {
      method: 'POST',
      body: JSON.stringify({ autoProcess: true })
    }
  );

  return response;
}
```

Настройте Vercel Cron:
```json
{
  "crons": [{
    "path": "/api/cron/process-tasks",
    "schedule": "0 9 * * *"
  }]
}
```

### Интеграция с Slack/Telegram

Добавьте уведомления о завершении обработки:

```typescript
// После обработки задачи
await fetch('https://hooks.slack.com/services/YOUR/WEBHOOK/URL', {
  method: 'POST',
  body: JSON.stringify({
    text: `✅ AI Agent completed: ${task.title}`
  })
});
```

## Метрики и Monitoring

Отслеживайте:
- Количество обработанных задач
- Среднее время обработки
- Success rate агентов
- ROI по каждому бизнесу

## Безопасность

- API key Anthropic хранится в `.env.local`
- Supabase используется для аутентификации
- GDPR compliance для немецкого рынка

## Стоимость

Anthropic Claude API:
- $3 за 1M input tokens
- $15 за 1M output tokens

Примерная стоимость обработки 1 задачи: **$0.05-0.10**

При обработке 100 задач/месяц: **$5-10/месяц**

ROI: **900%** (при доходе €21K/мес)

## Следующие шаги

1. ✅ Настроить автоматический запуск
2. ✅ Добавить уведомления
3. ✅ Интегрировать с CRM
4. ✅ A/B тестирование промптов агентов
5. ✅ Добавить больше специализированных агентов

---

Система готова к использованию! 🚀
